apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: curryware-wildcard-tls
  namespace: curryware
spec:
  secretName: curryware-tls
  dnsNames:
    - ubuntu-microk8s.curryware.org
  issuerRef:
    name: letsencrypt-prod
    kind: Issuer
